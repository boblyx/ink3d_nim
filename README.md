# ink3d_nim
Implementation of [ink3d](https://github.com/boblyx/ink3d) in [Nim](https://nim-lang.org/).

# Development
1. Use `nimble sync` to sync dependencies.
1. Build using `scripts/build.sh`
1. Output will be in `bin`
